[common]
bind_port = 15672  # 确保不被占用（1---->65533）
dashboard_port = 7600  #  确保不被占用（1---->65533）
dashboard_user = username # 随便起个名字，可以不写这个
dashboard_pwd = pwd            # 可以不写，这俩是浏览器打开设置frp的，没多大卵用
authentication_method = token
token = pwd
